const App = () => {

    const onClick = () => {
        alert("Helo Hiren");
    }
    return(
        <button onClick={onClick}>Submit me
        </button>
    );
}

export default App;

// This is for a aler calling in a same components;